define("timeline/models/change_history", function(require){
    var Backbone = require("backbone");

    var ChangeHistoryModel = Backbone.Model.extend({
        initialize : function() {
        },

    });

    return ChangeHistoryModel;
});