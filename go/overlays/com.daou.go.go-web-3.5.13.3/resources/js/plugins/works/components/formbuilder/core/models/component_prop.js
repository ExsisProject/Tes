define('works/components/formbuilder/core/models/component_prop', function(require) {
    var Backbone = require('backbone');
    var ComponentPropModel = Backbone.Model.extend({
        
    });
    
    return ComponentPropModel;
});