define('works/components/formbuilder/core/models/component', function(require) {
	
	var Backbone = require('backbone');
	
	/**
	 * 각 컴포넌트를 렌더링하기 위한 모델
	 */
	var ComponentModel = Backbone.Model.extend({

	});	
	
	return ComponentModel;
});