define('works/models/workflow_stat', function(require) {
	// dependency
	var Backbone = require('backbone');
	var GO = require('app');
	var when = require('when');

	var Stat = Backbone.Model.extend({
	});
	
	return Stat;
});