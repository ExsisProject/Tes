/**
 * @deprecated
 * constants 디렉토리 아래로 이동함. 하위호환성 때문에 남겨둠(언젠가 삭제 예정)
 */
define('works/component_type', function(require) {
    var ComponentType = require('works/constants/component_type');
    return ComponentType;
});