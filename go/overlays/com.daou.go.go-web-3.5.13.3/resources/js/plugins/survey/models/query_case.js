(function() {
    
    define(["backbone"], function(Backbone) {
        
        var QueryCaseModel = Backbone.Model.extend({
        });
        
        return QueryCaseModel;
        
    });
    
})();