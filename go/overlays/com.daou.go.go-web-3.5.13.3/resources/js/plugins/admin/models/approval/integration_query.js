define([
        'underscore',
        'backbone'
], function(
	    _,
	    Backbone
) {
	
	
	var IntegrationQueryModel = Backbone.Model.extend({
		
		initialize : function() {
		}
	});
	
	return IntegrationQueryModel;
});