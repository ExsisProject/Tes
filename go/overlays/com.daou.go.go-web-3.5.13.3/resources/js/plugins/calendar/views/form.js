(function() {
    
    define([
        "backbone" 
    ], 
    
    function( 
        Backbone 
    ) {
        /**
         * TODO: 리팩토링을 위한 사전 작업입니다.
         */
        var CalendarFormView = Backbone.View.extend({
            
        });
        
        return CalendarFormView;
    });
    
})();